import type { Metadata } from 'next'
import SiteNav from '@/components/SiteNav'

export const metadata: Metadata = { title: 'Security', description: 'Security boundaries and safeguards for Total offline-first accounting software.', alternates: { canonical: '/security' } }

export default function SecurityPage(): React.JSX.Element {
  return <><SiteNav /><main className="wrap legal-page"><p className="eyebrow">Security at Total</p><h1 className="serif">Local by default. Reviewable by design.</h1><p className="lede">The core accounting path works without an account or cloud database. Network features are optional and visible.</p><section><h2>Product safeguards</h2><ul><li>One transactional SQLite database per company, with migrations, integrity checks, WAL recovery and verified backups.</li><li>Integer paise and balanced voucher validation at the main-process boundary.</li><li>Human-reviewed AI proposals; AI cannot directly post vouchers or change deterministic report calculations.</li><li>Encrypted local secrets, role-based access, approval controls, a tamper-evident audit chain and scoped exports.</li><li>Signed build requirements, dependency policy, install/upgrade tests, performance budgets and release evidence in CI.</li></ul></section><section><h2>Your security responsibilities</h2><p>Keep your operating system updated, restrict device access, use strong local passwords, verify backup restores, review exports before sharing, and revoke provider keys you no longer use. Do not open the same live company database from multiple synced computers.</p></section><section><h2>Report a vulnerability</h2><p>Email <a href="mailto:total@irminflow.com?subject=Total%20security%20report">total@irminflow.com</a> with “security report” in the subject. Include affected version, reproduction steps and impact. Do not include real customer books or credentials. We will acknowledge a credible report and coordinate remediation before public disclosure.</p></section><section><h2>Current boundary</h2><p>NIC live filing and GST portal connectivity are experimental and are not included in the production-ready claim. Offline books, calculations and reviewed exports remain independent of those services.</p></section></main></>
}
